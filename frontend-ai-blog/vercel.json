{
    "buildCommand": "npm run build",
    "outputDirectory": "build",
    "rewrites": [
      {
        "source": "/api/:path*", 
        "destination": "https://recursivai-api.onrender.com/:path*"
      }
    ]
  }