name: Keep Render App Awake

# DISABLED: Allow backend to spin down to save free tier hours
# on:
#   schedule:
#     - cron: '*/7 * * * *'  # Run every 10 minutes
#   workflow_dispatch:         # Allow manual triggering

on:
  workflow_dispatch:         # Allow manual triggering only (disabled schedule)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl "https://recursivai.onrender.com/posts/healthcheck" || echo "Ping complete"
