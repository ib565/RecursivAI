name: Keep Render App Awake

on:
  schedule:
    - cron: '*/13 * * * *'  # Run every 13 minutes
  workflow_dispatch:         # Allow manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl "https://recursivai.onrender.com/health" || echo "Ping complete"
